Space Concordia Robotics Division Intro Task 

Setup instructions:

1) `colcon build` at root
2) Source the path for install/setup.bash
3) Run `cpp_pkg` or `py_pkg` packages with node `listener` or `talker` for both packages
